- name: Configure docker
  hosts: instance-python-vm
  become: true
  tasks:
    - name: Install docker
      apt:
        name: 
          - docker.io
        state: present
    - name: lance le docker run
      shell: docker run -p 80:5000 hizzo/my-image-python -d